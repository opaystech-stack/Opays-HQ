# Opays HQ — Modules V2 : Design, Factures, Prospects, Marketing

> **Pour Hermes :** Implémenter tâche par tâche via `delegate_task`. Chaque tâche = 2-5 min. Commit après chaque tâche. Push final sur GitHub.

**Goal :** Améliorer le design responsive (surtout PC) de tous les modules, ajouter la génération de factures avec templates, ajouter le module Prospects, et créer des templates marketing.

**Architecture :** Frontend React 19 + TanStack Router + Tailwind 4 + Recharts. Backend Express 5 + SQLite. Les factures sont générées côté serveur en HTML/CSS imprimable, servies via une route dédiée. Les templates marketing sont des pages HTML statiques servies depuis le serveur.

**Tech Stack :** React 19, TanStack Router, Tailwind 4, Recharts, Express 5, SQLite, HTML/CSS print-ready

---

## Phase 1 — Design PC Amélioré (tous les modules)

### Task 1.1: Dashboard — Layout PC premium

**Objectif :** Transformer le dashboard en un vrai tableau de bord CEO avec grille 3 colonnes sur PC, widgets métier, graphiques Recharts.

**Fichiers :**
- Modifier : `src/routes/_app.app.dashboard.tsx`
- Modifier : `src/styles.css` (ou le CSS Tailwind)

**Design attendu :**
```
┌─────────────────────────────────────────────────────┐
│  Bonjour, Fenelon · CEO                             │
│  Vue d'ensemble du 1er juillet 2026                 │
├──────────┬──────────┬──────────┬──────────┬──────────┤
│ Projets  │ Tâches   │ Urgentes │ Employés │Trésorerie│
│ actifs   │ en cours │          │          │  nette   │
│    12    │    47    │    3     │    10    │ 45 200 $ │
├──────────┴──────────┴──────────┴──────────┴──────────┤
│ ┌──────────────────┐  ┌──────────────────────────────┐│
│ │ Tâches récentes  │  │ Graphique Trésorerie         ││
│ │ • Déploiement... │  │   📈 (Recharts LineChart)    ││
│ │ • Audit CRM...   │  │                              ││
│ │ • Réunion CEO... │  │                              ││
│ └──────────────────┘  └──────────────────────────────┘│
│ ┌──────────────────┐  ┌──────────────────────────────┐│
│ │ Projets actifs   │  │ Activité récente              ││
│ │ • Opays HQ       │  │ • Nouveau lead ajouté        ││
│ │ • Site vitrine   │  │ • Tâche terminée              ││
│ │ • CloddsBot      │  │ • Facture #001 émise         ││
│ └──────────────────┘  └──────────────────────────────┘│
└─────────────────────────────────────────────────────┘
```

**Code :** Remplacer le contenu de `DashboardPage` par :

```tsx
// Layout responsive : 5 stat-cards en ligne sur PC, 2 sur mobile
// 2 colonnes pour les widgets (tâches + graphique tréso)
// 2 colonnes pour (projets + activité)
// Graphique Recharts LineChart pour la trésorerie (mock data si vide)
// Section "Activité récente" avec les 5 dernières actions
```

### Task 1.2: CRM/Leads — Design Kanban + Tableau

**Objectif :** Ajouter une vue Kanban visuelle pour les leads (drag & drop entre colonnes new → contacted → audit → proposal → won/lost) + améliorer le tableau.

**Fichiers :**
- Modifier : `src/routes/_app.app.leads.tsx`
- Ajouter : `src/components/LeadKanban.tsx`

**Design :** Vue toggle Tableau ↔ Kanban. Le Kanban montre 6 colonnes avec le nombre de leads dans chaque. Drag & drop avec `@dnd-kit/core` (déjà installé).

### Task 1.3: Trésorerie — Graphiques et filtres

**Objectif :** Ajouter un graphique Recharts (évolution du solde dans le temps), filtres par mois/trimestre, catégories avec couleurs.

**Fichiers :**
- Modifier : `src/routes/_app.app.treasury.tsx`

**Design :** Stats cards + graphique line + tableau filtré par période. Catégories avec badges colorés.

### Task 1.4: RH/Team — Design équipe

**Objectif :** Améliorer la page RH avec cartes employés (photo/avatar, nom, rôle, salaire, perf), graphique d'equity, organigramme simple.

**Fichiers :**
- Modifier : `src/routes/_app.app.rh.tsx`

**Design :** Grille de cartes employés (3 colonnes PC, 1 mobile). Chaque carte montre l'avatar (initiale), nom, rôle, badge salaire, jauge de performance. Graphique equity (Recharts).

### Task 1.5: Coffre-fort — Design documents

**Objectif :** Améliorer la page Vault avec grille de documents (icônes, aperçus), catégories (Contrats, Factures, Marketing, RH).

**Fichiers :**
- Modifier : `src/routes/_app.app.vault.tsx`

**Design :** Grille de dossiers catégorisés + liste des documents. Upload drag & drop amélioré.

### Task 1.6: Projets — Design amélioré

**Objectif :** Améliorer la page Projets avec cartes visuelles (barre de progression, tags, marges), vue Kanban optionnelle.

**Fichiers :**
- Modifier : `src/routes/_app.app.projects.tsx`

### Task 1.7: Tâches — Design amélioré

**Objectif :** Améliorer la page Tâches avec Kanban (todo → in_progress → review → done), filtres par projet/assigné/priorité.

**Fichiers :**
- Modifier : `src/routes/_app.app.tasks.tsx`

### Task 1.8: Calendrier — Design amélioré

**Objectif :** Améliorer la page Calendrier avec vue mois/semaine, code couleur par type d'événement.

**Fichiers :**
- Modifier : `src/routes/_app.app.calendar.tsx`

### Task 1.9: Agents IA — Design amélioré

**Objectif :** Améliorer le chat IA avec bulles stylisées, suggestions rapides, historique.

**Fichiers :**
- Modifier : `src/routes/_app.app.agents.tsx`

### Task 1.10: Paramètres — Design amélioré

**Objectif :** Améliorer la page Settings avec onglets (Équipe, Rôles, Equity, Sécurité), design plus clair.

**Fichiers :**
- Modifier : `src/routes/_app.app.settings.tsx`

---

## Phase 2 — Module Factures

### Task 2.1: Base de données — Table invoices

**Objectif :** Ajouter la table `invoices` dans le schéma SQLite.

**Fichiers :**
- Modifier : `server/db.ts` (dans `initSchema()`)

**Code SQL :**
```sql
CREATE TABLE IF NOT EXISTS invoices (
  id TEXT PRIMARY KEY,
  invoice_number TEXT UNIQUE NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('sale','proforma','credit_note','debit_note','quote')),
  status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','sent','paid','overdue','cancelled')),
  client_name TEXT NOT NULL,
  client_email TEXT,
  client_address TEXT,
  client_tax_id TEXT,
  items TEXT NOT NULL DEFAULT '[]',  -- JSON array
  subtotal REAL NOT NULL,
  tax_rate REAL DEFAULT 0,
  tax_amount REAL DEFAULT 0,
  discount_percent REAL DEFAULT 0,
  discount_amount REAL DEFAULT 0,
  total REAL NOT NULL,
  currency TEXT DEFAULT 'USD',
  notes TEXT,
  terms TEXT,
  due_date TEXT,
  issued_date TEXT,
  paid_date TEXT,
  created_by TEXT REFERENCES users(id),
  project_id TEXT REFERENCES projects(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);
```

### Task 2.2: Modèle backend — CRUD factures

**Objectif :** Ajouter les fonctions CRUD pour les factures dans `server/models.ts`.

**Fichiers :**
- Modifier : `server/models.ts`

**Fonctions :**
- `createInvoice(data)` — génère un numéro auto (INV-2026-0001), calcule totaux
- `getInvoices(userId, roleName)` — liste avec filtres par rôle
- `getInvoiceById(id)` — une facture
- `updateInvoiceStatus(id, status)` — changer statut
- `deleteInvoice(id)` — soft delete ou réel

### Task 2.3: Routes API — /api/invoices

**Objectif :** Créer les routes REST pour les factures.

**Fichiers :**
- Créer : `server/routes/invoices.ts`
- Modifier : `server/index.ts` (ajouter `app.use('/api/invoices', invoiceRoutes)`)

**Routes :**
- `GET /api/invoices` — liste
- `POST /api/invoices` — créer
- `GET /api/invoices/:id` — détail
- `PATCH /api/invoices/:id/status` — changer statut
- `DELETE /api/invoices/:id` — supprimer
- `GET /api/invoices/:id/pdf` — générer HTML imprimable

### Task 2.4: Générateur PDF/HTML — Template facture

**Objectif :** Créer un template HTML/CSS imprimable pour les factures, servi via la route `/api/invoices/:id/pdf`.

**Fichiers :**
- Créer : `server/templates/invoice.html` (template HTML avec CSS print)
- Modifier : `server/routes/invoices.ts` (route GET /:id/pdf)

**Template attendu :**
```
┌─────────────────────────────────────────────────────┐
│  OPAYS TECH                          FACTURE #001   │
│  Avenue de la Libération                            │
│  Goma, RDC                                          │
│  contact@opays.tech                                 │
│                                                     │
│  Client : ACME SARL                    Date :       │
│  Email : client@acme.com              Échéance :    │
│                                                     │
├──────────────┬───────┬────────┬────────┬────────────┤
│ Description  │ Qté   │ PU     │ Total  │            │
├──────────────┼───────┼────────┼────────┼────────────┤
│ Développement│   1   │ 5 000  │ 5 000  │            │
│ Hébergement  │  12   │   150  │ 1 800  │            │
├──────────────┴───────┴────────┴────────┴────────────┤
│                                        Sous-total   │
│                                        Remise 0%    │
│                                        TVA 0%       │
│                                        TOTAL 6 800 $│
├─────────────────────────────────────────────────────┤
│  Conditions : Paiement sous 30 jours                 │
│  IBAN : CD...                                        │
│                                                     │
│  Merci de votre confiance !                          │
│  Opays Tech — contact@opays.tech                     │
└─────────────────────────────────────────────────────┘
```

### Task 2.5: Types de factures

**Objectif :** Définir et implémenter 5 types de factures :

| Type | Nom | Usage |
|------|-----|-------|
| `sale` | Facture de vente | Standard — prestation livrée |
| `proforma` | Facture proforma | Devis engageant, avant prestation |
| `credit_note` | Avoir | Remboursement / annulation |
| `debit_note` | Note de débit | Complément / ajustement |
| `quote` | Devis | Proposition commerciale |

Chaque type a un préfixe de numérotation différent :
- FACT-2026-0001 (sale)
- PRO-2026-0001 (proforma)
- AVOIR-2026-0001 (credit_note)
- ND-2026-0001 (debit_note)
- DEV-2026-0001 (quote)

### Task 2.6: Frontend — Page Factures

**Objectif :** Créer la page `/app/invoices` avec liste, création, détail, export PDF.

**Fichiers :**
- Créer : `src/routes/_app.app.invoices.tsx`
- Modifier : `src/routes/_app.tsx` (ajouter nav item)
- Modifier : `src/lib/api.ts` (ajouter fonctions API)
- Modifier : `src/routeTree.gen.ts` (si généré automatiquement)

**Design :** Stats cards (Total impayé, Factures en attente, Chiffre d'affaires) + tableau des factures + bouton "Nouvelle facture" → formulaire avec sélection de type, client, lignes (description, quantité, prix unitaire, TVA).

### Task 2.7: Sidebar — Ajouter Factures

**Objectif :** Ajouter "Factures" dans la sidebar, section Gestion, visible par CEO, COO, Admin, Sales.

**Fichiers :**
- Modifier : `src/routes/_app.tsx`

**Code :**
```tsx
{ to: '/app/invoices', label: 'Factures', icon: FileText, roles: ['admin', 'ceo', 'coo', 'sales'] },
```

---

## Phase 3 — Module Prospects

### Task 3.1: Renommer "CRM / Leads" en "Prospects"

**Objectif :** Le module CRM/Leads actuel est trop générique. Le renommer en "Prospects" avec un pipeline visuel dédié.

**Fichiers :**
- Modifier : `src/routes/_app.tsx` (label → "Prospects")
- Modifier : `src/routes/_app.app.leads.tsx` (titres → "Prospects")

**Design du pipeline Prospects :**
```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Nouveau  │→ │ Contacté │→ │  Audit   │→ │Proposition│→ │  Gagné   │  │  Perdu   │
│    3     │  │    5     │  │    2     │  │    1     │  │    0     │  │    1     │
└──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘
```

### Task 3.2: Ajouter champs Prospects enrichis

**Objectif :** Ajouter des champs spécifiques aux prospects : secteur d'activité, taille d'entreprise, source (référence, LinkedIn, site web, salon), notes d'appel, prochaine action.

**Fichiers :**
- Modifier : `server/db.ts` (migration `ensureColumn` pour leads)
- Modifier : `server/routes/leads.ts` (accepter nouveaux champs)
- Modifier : `src/routes/_app.app.leads.tsx` (afficher nouveaux champs)

**Nouveaux champs :**
- `industry` TEXT — secteur (Tech, Finance, Santé, Éducation, etc.)
- `company_size` TEXT — TPE, PME, ETI, Grand compte
- `source` TEXT — Référence, LinkedIn, Site web, Salon, Appel entrant
- `next_action` TEXT — prochaine action planifiée
- `next_action_date` TEXT — date de la prochaine action
- `call_notes` TEXT — historique des appels

---

## Phase 4 — Templates Marketing

### Task 4.1: Base de données — Table marketing_templates

**Objectif :** Créer la table pour stocker les templates marketing.

**Fichiers :**
- Modifier : `server/db.ts`

**SQL :**
```sql
CREATE TABLE IF NOT EXISTS marketing_templates (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('email','proposal','brochure','presentation','social_media','quote')),
  description TEXT,
  content TEXT NOT NULL,  -- HTML template with {{placeholders}}
  variables TEXT DEFAULT '[]',  -- JSON array of variable names
  created_by TEXT REFERENCES users(id),
  created_at TEXT DEFAULT (datetime('now')),
  updated_at TEXT DEFAULT (datetime('now'))
);
```

### Task 4.2: Routes API — Templates marketing

**Objectif :** CRUD pour les templates marketing.

**Fichiers :**
- Créer : `server/routes/marketing.ts`
- Modifier : `server/index.ts`

**Routes :**
- `GET /api/marketing/templates` — liste
- `POST /api/marketing/templates` — créer
- `PUT /api/marketing/templates/:id` — modifier
- `DELETE /api/marketing/templates/:id` — supprimer
- `GET /api/marketing/templates/:id/preview` — aperçu HTML
- `POST /api/marketing/templates/:id/render` — générer avec variables

### Task 4.3: Templates préremplis

**Objectif :** Créer 5 templates marketing de base dans le seed.

**Fichiers :**
- Modifier : `server/seed.ts` (dans `seedAgents()` ou nouvelle fonction)

**Templates :**
1. **Email de prospection** — "Bonjour {{client_name}}, nous avons une solution..."
2. **Proposition commerciale** — Template de devis/offre formaté
3. **Brochure entreprise** — Présentation Opays Tech
4. **Post LinkedIn** — Annonce de projet / recrutement
5. **Lettre de motivation** — Template pour candidatures

### Task 4.4: Frontend — Page Marketing

**Objectif :** Créer la page `/app/marketing` pour gérer et générer les templates.

**Fichiers :**
- Créer : `src/routes/_app.app.marketing.tsx`
- Modifier : `src/routes/_app.tsx` (nav item)
- Modifier : `src/lib/api.ts`
- Modifier : `src/routeTree.gen.ts`

**Design :** Grille de templates avec aperçu, bouton "Utiliser" → éditeur avec champs variables → générer → copier/télécharger.

### Task 4.5: Sidebar — Ajouter Marketing

**Objectif :** Ajouter "Marketing" dans la sidebar, section Gestion, visible par CEO, Sales.

**Fichiers :**
- Modifier : `src/routes/_app.tsx`

---

## Phase 5 — Déploiement

### Task 5.1: Commit & Push

**Objectif :** Commiter toutes les modifications et pousser sur GitHub.

```bash
git add -A
git commit -m "feat: modules V2 - design PC, factures, prospects, marketing"
git push origin main
```

### Task 5.2: Rebuild & Redéployer

**Objectif :** Rebuild l'image Docker et redéployer sur le VPS.

```bash
# Sur le VPS
docker service update --force --image opays-hq-xae4lm:latest opays-hq-xae4lm
```

### Task 5.3: Vérification

**Objectif :** Vérifier que tout fonctionne.

```bash
curl -s https://hq.opays.io/api/health
curl -s -X POST https://hq.opays.io/api/auth/login -H "Content-Type: application/json" -d '{"email":"lamsasfenelon@gmail.com","password":"admin123"}'
curl -s https://hq.opays.io/api/invoices -H "Authorization: Bearer <token>"
```

---

## Résumé des fichiers modifiés/créés

| Fichier | Action | Phase |
|---------|--------|-------|
| `src/routes/_app.app.dashboard.tsx` | Modifier | 1.1 |
| `src/routes/_app.app.leads.tsx` | Modifier | 1.2, 3.1, 3.2 |
| `src/components/LeadKanban.tsx` | Créer | 1.2 |
| `src/routes/_app.app.treasury.tsx` | Modifier | 1.3 |
| `src/routes/_app.app.rh.tsx` | Modifier | 1.4 |
| `src/routes/_app.app.vault.tsx` | Modifier | 1.5 |
| `src/routes/_app.app.projects.tsx` | Modifier | 1.6 |
| `src/routes/_app.app.tasks.tsx` | Modifier | 1.7 |
| `src/routes/_app.app.calendar.tsx` | Modifier | 1.8 |
| `src/routes/_app.app.agents.tsx` | Modifier | 1.9 |
| `src/routes/_app.app.settings.tsx` | Modifier | 1.10 |
| `server/db.ts` | Modifier | 2.1, 3.2, 4.1 |
| `server/models.ts` | Modifier | 2.2 |
| `server/routes/invoices.ts` | Créer | 2.3 |
| `server/index.ts` | Modifier | 2.3, 4.2 |
| `server/templates/invoice.html` | Créer | 2.4 |
| `src/routes/_app.app.invoices.tsx` | Créer | 2.6 |
| `src/lib/api.ts` | Modifier | 2.6, 4.4 |
| `src/routes/_app.tsx` | Modifier | 2.7, 3.1, 4.5 |
| `server/routes/leads.ts` | Modifier | 3.2 |
| `server/routes/marketing.ts` | Créer | 4.2 |
| `server/seed.ts` | Modifier | 4.3 |
| `src/routes/_app.app.marketing.tsx` | Créer | 4.4 |

---

## Risques et questions

- **Taille du build** : L'ajout de Recharts et @dnd-kit augmente le bundle. Vérifier après build.
- **Migration DB** : Les nouvelles colonnes sont ajoutées via `ensureColumn` (idempotent). Pas de risque de perte de données.
- **Factures PDF** : Pas de librairie PDF — on utilise HTML/CSS print. L'utilisateur imprime via le navigateur (Ctrl+P). Si besoin de vrais PDFs plus tard, ajouter `puppeteer` ou `pdfkit`.
- **RouteTree** : Vérifier si TanStack Router génère automatiquement `routeTree.gen.ts` ou si modification manuelle nécessaire.
